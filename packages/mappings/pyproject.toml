[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "simplifyr-mappings"
version = "0.1.0"
description = "Simplifyr field mapping and semantic normalization engine"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]